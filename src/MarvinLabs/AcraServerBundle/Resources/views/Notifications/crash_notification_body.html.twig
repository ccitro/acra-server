Hey,

There is a new crash (issue #{{ crash.issueId }}) for your application {{ crash.packageName }}.  There have been {{ numberOfCrashes }} crash(es) for this issue.

Link
--
{{ protocol }}:{{ url('_crash_details', {'id': crash.id}, true) }}

Stacktrace:
--
{{ crash.shortStackTrace }}

